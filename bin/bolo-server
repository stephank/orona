#!/usr/bin/env node

// FIXME: Correct way to find the lib dir and add it to require.paths?
bolo = require('../lib/server');
puts = require('sys').puts;

bolo().listen(8124);
puts("Bolo server listening on port 8124.");
